{
  "name": "gyaonextension",
  "version": "1.0.0",
  "description": "chrome extension of gyaon",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist/*",
    "tsc": "./node_modules/.bin/tsc",
    "copy": "cp ./src/manifest.json ./dist && cp -r ./src/pages ./dist/pages && cp -r ./src/icons ./dist/icons",
    "build": "npm run clean && npm run copy && npm run tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stkay/GyaonExtension.git"
  },
  "author": "hykwtakumin <hykwtakumin@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/stkay/GyaonExtension/issues"
  },
  "homepage": "https://github.com/stkay/GyaonExtension#readme",
  "dependencies": {},
  "devDependencies": {
    "@types/chrome": "^0.0.68",
    "typescript": "^2.9.1"
  }
}
